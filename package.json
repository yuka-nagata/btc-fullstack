{
  "name": "btc-fullstack",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm i && cd front && npm install && npm run build && rm -rf ../public && cp -r dist ../public",
    "migrate-latest": "npx knex migrate:latest --knexfile db/knexfile.js",
    "migrate-down": "npx knex migrate:down --knexfile db/knexfile.js",
    "seed-data": "npx knex seed:run --knexfile db/knexfile.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "knex": "^3.1.0",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  }
}
